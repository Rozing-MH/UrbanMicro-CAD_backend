# 后端变更日志

生成日期：2026-05-29
项目：UrbanMicro-CAD Backend
范围：按设计文档和后端开发计划创建 Spring Boot 后端 MVP，并对齐当前前端 API 契约。

## v0.3.2 变更记录

变更时间：2026-06-04 20:30:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 模板 snapshotData 键名规范化 | `TemplateConverter` 新增 `normalizeSnapshotKeys()` 方法，将数据库种子数据中的 `"topology"` → `"topologyData"`、`"rules"` → `"ruleData"` 自动转换，对齐前端 `ProjectPayload` 契约；仅当旧键存在且新键不存在时才转换，已有规范键名的不受影响。 |
| 已完成 | toDto/toCustomDto 统一规范化 | 两个 DTO 转换方法均经过 `normalizeSnapshotKeys()` 处理，确保所有模板 API 响应键名一致。 |
| 已完成 | Converter 单元测试 | 新增 `TemplateConverterTest`，6 个测试覆盖：旧键名规范化、已有规范键不重复转换、两种键并存时优先规范键、null 安全、toCustomDto 同样规范化、profile 从规范化后数据提取。 |
| 已验证 | Maven 全量测试 | 执行 `mvn test`，100 个测试全部通过（原 94 + 新增 6）。 |
| 已验证 | 编译检查 | 执行 `mvn compile` 通过。 |

## v0.3.1 变更记录

变更时间：2026-06-03 09:30:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | PostgreSQL 枚举 TypeHandler | 新增 `PgEnumTypeHandler`，将 Java String 通过 `ps.setObject(i, value, Types.OTHER)` 映射为 PostgreSQL 枚举类型；修复 `enum_user_role` 和 `enum_template_category` 列写入时类型不匹配错误。 |
| 已完成 | UUID TypeHandler 修复 | `UuidTypeHandler.setNonNullParameter()` 从 `ps.setString()` 改为 `ps.setObject(i, parameter, Types.OTHER)`，修复 PostgreSQL UUID 列写入时"column is of type uuid but expression is of type character varying"错误。 |
| 已完成 | 实体 TypeHandler 注解 | `SysUser.role` 加 `@TableField(typeHandler = PgEnumTypeHandler.class)` + `autoResultMap=true`；`ProjectTemplate.id` 加 `@TableField(typeHandler = UuidTypeHandler.class)`，`ProjectTemplate.category` 加 `@TableField(typeHandler = PgEnumTypeHandler.class)`；`Project.id`、`ProjectSnapshot.id/projectId`、`EvaluationReport.id/projectId` 加 `@TableField(typeHandler = UuidTypeHandler.class)`。 |
| 已完成 | Redis 本地排除 | `application-local.yml` 新增 `spring.autoconfigure.exclude` 排除 RedisAutoConfiguration，本地开发无 Redis 时不影响启动和健康检查。 |
| 已完成 | 配置默认值 | `application.yml` 中 `spring.datasource.*` 和 `app.jwt.secret` 增加 `:` 默认值语法，支持不带环境变量启动（配合 local profile 覆盖）。 |
| 已完成 | 前端 reportApi 分页兼容 | 前端 `reportApi.list()` 从期望 `ApiResponse<ReportSummary[]>` 改为兼容 `PageResponse<ReportSummary>`，与后端分页响应格式对齐。 |
| 已完成 | 前端 SnapshotInfo.id 类型修正 | 前端 `SnapshotInfo.id` 和 `SnapshotInfoDTO.id` 从 `number` 改为 `string`，与后端 `ProjectSnapshotDTO.id`（UUID）对齐。 |
| 已完成 | application-local.yml 体系 | 新增 `src/main/resources/application-local.yml`（Supabase 连接配置，已加入 .gitignore）和 `application-local.yml.example`（模板文件，提示用户复制填写）；`.gitignore` 新增 `application-local.yml`。 |
| 已完成 | 冒烟测试脚本 | 新增 `smoke-test.ps1`，覆盖注册、登录、me、创建工程、列表、详情、保存快照、模板列表、报表列表、删除工程 10 项 API 验证。 |
| 已验证 | 后端 API 冒烟测试 | 连接真实 Supabase 数据库，10 项 API 全部通过。 |
| 已验证 | 前端浏览器联调 | 登录→Dashboard（用户名显示正确）→创建工程→编辑器加载（Three.js canvas 正常）→Ctrl+S 保存（toast"项目已保存"）→Dashboard 显示项目→打开已有项目成功。 |
| 已验证 | 编译检查 | `mvn compile` 通过；前端 `npm run type-check` + `npm run build` 通过。 |

## v0.3.0 变更记录

变更时间：2026-06-02 21:30:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | Service 接口化 | 新增 IAuthService/IProjectService/ITemplateService/IReportService 接口；4 个 Service 实现类 implements 接口并加 @Override；4 个 Controller 注入接口类型而非具体类。对齐设计文档 3.2 后端包图 SOLID 原则。 |
| 已完成 | Converter 转换层 | 新增 ProjectConverter/TemplateConverter/ReportConverter @Component 类；ProjectService 中 toDto/toSummaryDto/toSnapshotDto 提取到 ProjectConverter；TemplateService 中 toDto/toCustomDto 提取到 TemplateConverter；ReportService 中 toSummary/toDetail 提取到 ReportConverter。对齐设计文档 3.2 Converter 层。 |
| 已完成 | Redis 依赖 | pom.xml 新增 spring-boot-starter-data-redis；RedisConfig 条件装配（@ConditionalOnProperty）；生产环境自动启用 Redis，开发/测试环境回退内存实现。 |
| 已完成 | TokenBlacklist 双实现 | TokenBlacklist 重构为接口 + InMemoryTokenBlacklist/RedisTokenBlacklist 内部类；@ConditionalOnBean(StringRedisTemplate) 自动切换；Redis 实现使用 TTL 自动过期，无需手动清理。 |
| 已完成 | Docker Compose Redis | docker-compose.yml 新增 redis:7-alpine 服务（AOF 持久化、128MB 上限、LRU 淘汰、healthcheck）；app 容器注入 SPRING_DATA_REDIS_HOST/PORT；新增 redisdata volume。 |
| 已完成 | 生产 Redis 配置 | application-prod.yml 新增 spring.data.redis.* 配置（host/port/password 从环境变量读取、lettuce 连接池调优）。 |
| 已完成 | 开发计划同步 | 新增阶段 7「架构对齐与生产增强」5 项任务均标记 [x]；修正「近期完成定义」中已完成的 PDF/限流/Docker 等项。 |
| 已验证 | Maven 全量测试 | 执行 `mvn test`，94 个测试全部通过。 |
| 已验证 | 编译检查 | 执行 `mvn compile` 通过。 |

## v0.2.1 变更记录

变更时间：2026-06-02 20:25:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 快照并发安全 | `saveSnapshot()` 和 `restoreSnapshot()` 使用 `SELECT ... FOR UPDATE` 行锁防止并发保存冲突；版本递增依赖数据库触发器 `trg_project_version_increment`，保存后重新读取获取触发器更新后的 version 值。 |
| 已完成 | ProjectMapper.selectOneForUpdate | 新增自定义 SQL 方法，查询工程并加行锁，必须在 `@Transactional` 事务内调用。 |
| 已完成 | 数据库迁移 | Supabase 执行 `ALTER TABLE prj_template ADD COLUMN user_id BIGINT REFERENCES sys_user(id) ON DELETE CASCADE`；新增 `idx_prj_template_user_id` 部分索引（WHERE user_id IS NOT NULL）。 |
| 已完成 | 生产配置完善 | `application-prod.yml` 新增 HikariCP 连接池调优（最大10连接、泄漏检测）、日志级别收敛（root=WARN、业务=INFO）、Actuator 仅暴露 health 且不显示详情、限流参数可配。 |
| 已完成 | Docker 中文字体 | Dockerfile 安装 `font-noto-cjk` Alpine 包；`PdfReportService` 新增系统字体路径回退（/app/fonts/、/usr/share/fonts/）；docker-compose 挂载 `./fonts:/app/fonts:ro` 卷、启用 `SPRING_PROFILES_ACTIVE: prod`。 |
| 已完成 | 模板删除接口 | 新增 `DELETE /api/templates/{id}` 端点，仅模板所有者可删除，系统模板不可删除，使用软删除。 |
| 已完成 | 乐观锁冲突测试 | `ProjectServiceTest` 新增 2 个测试：saveSnapshot 工程不存在抛 NOT_FOUND、restoreSnapshot 工程不存在抛 NOT_FOUND。 |
| 已完成 | 开发计划修正 | 阶段 5 PDF 导出从 `[ ]` 更新为 `[x]`（v0.1.6 已完成）；阶段 6 第 10 项更新为 `[-]`（测试已完成，联调待执行）。 |
| 已验证 | Maven 全量测试 | 执行 `mvn test`，94 个测试全部通过（原 92 + 新增 2）。 |
| 已验证 | 编译检查 | 执行 `mvn compile` 通过。 |

## v0.2.1 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 前后端联调未执行 | 分页、乐观锁冲突响应等变更需前端验证。 | 配置环境变量后做 smoke test。 |
| 限流/黑名单仍为内存方案 | 单实例有效，多实例部署状态不共享。 | 后续引入 Redis + Bucket4j。 |
| Docker 构建未验证 | Dockerfile 已更新但未执行 `docker compose up --build`。 | 本地构建验证。 |

## v0.2.0 变更记录

变更时间：2026-06-02 19:25:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 分页基础设施 | 新增 `PageResponse<T>` 通用分页响应 record（含 records/total/page/size 字段），工厂方法 `from(Page<R>, Function<R,T>)` 从 MyBatis-Plus Page 转换。 |
| 已完成 | 项目列表分页 | `GET /api/projects` 新增 `page`/`size` 查询参数（默认 page=1, size=20），返回 `PageResponse<ProjectSummaryDTO>`；`page` 最小值 1，`size` 范围 1-100。 |
| 已完成 | 快照列表分页 | `GET /api/projects/{id}/snapshots` 新增 `page`/`size` 查询参数，返回 `PageResponse<ProjectSnapshotDTO>`。 |
| 已完成 | 报表列表分页 | `GET /api/reports?projectId=...` 新增 `page`/`size` 查询参数，返回 `PageResponse<ReportSummary>`。 |
| 已完成 | ProjectSummaryDTO | 新增轻量项目列表 DTO（不含 topologyData/ruleData 大字段），Dashboard 页面只需元数据。前端 `dtoToProjectMeta()` 已显式丢弃大字段，此变更为安全优化。 |
| 已完成 | 快照版本加载 | 新增 `GET /api/projects/{id}/snapshots/{version}` 端点，按整数版本号加载快照数据（只读），返回完整 `ProjectDTO`（含 topologyData/ruleData）。兼容 topologyData/ruleData 与 topology/rules 两种键名。 |
| 已完成 | ProjectSnapshotDTO 扩展 | 新增可空 `snapshotData` 字段；列表端点不填充此字段，版本加载通过独立 ProjectDTO 返回。 |
| 已完成 | 用户自定义模板 | 新增 `POST /api/templates` 端点，保存用户自定义断面模板到 `prj_template`；category 强制 CUSTOM（防权限提升）；profile 校验必须含 lanes 数组；同用户同名模板去重。 |
| 已完成 | 模板用户归属 | `ProjectTemplate` 实体新增 `userId` 字段（nullable，NULL=系统模板）；`list()` 返回系统模板 + 当前用户自定义模板；`get()` 同理过滤。 |
| 已完成 | 模板 assets fallback | 新增 `GET /api/templates/assets` fallback 端点，委托 `list()` 方法。 |
| 已完成 | LosCalculator | 新增纯工具类，`averageDelay()` 从 intersectionLos JSONB 计算平均控制延误，`losGrade()` 按 HCM 第6版阈值映射 LOS 等级（A≤10→B≤20→C≤35→D≤55→E≤80→F>80）；防御性处理 null/空数据。 |
| 已完成 | 真实报表指标 | `ReportService.toSummary()` 和 `detail()` 中 networkLOS/averageDelay 不再硬编码 "C"/0，改为从 intersectionLos 动态计算。 |
| 已完成 | 分页参数校验 | `page` 参数限制最小值 1，`size` 参数限制范围 1-100（Math.clamp）。 |
| 已验证 | Maven 全量测试 | 执行 `mvn test`，92 个测试全部通过（原 79 + 新增 13）。 |
| 已验证 | 编译检查 | 执行 `mvn compile` 通过。 |
| 已验证 | 代码审查 | Java 代码审查发现 2 CRITICAL + 5 HIGH，均已修复：page 参数未校验、模板重名检查、null 用户检查。 |

## v0.2.0 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 数据库迁移未执行 | `prj_template` 表需新增 `user_id BIGINT` 列。 | 在 Supabase 执行：`ALTER TABLE prj_template ADD COLUMN user_id BIGINT REFERENCES sys_user(id) ON DELETE CASCADE`。 |
| 快照版本号未自增 | `saveSnapshot()` 依赖数据库触发器更新 version，当前代码未手动递增。并发保存可能产生相同版本号快照。 | 后续在 Service 层显式 `setVersion(getVersion() + 1)` + 乐观锁。 |
| 前后端联调未执行 | 分页响应格式变更需前端验证。 | 前端 `PageResponse<T>` 已兼容 records/content/items，联调确认。 |

## v0.1.6 变更记录

变更时间：2026-06-02 16:55:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | PDF 报表导出 | 新增 `PdfReportService`，基于 OpenPDF 2.2.2（LGPL）生成图文评估简报；PDF 含封面页、评估概要表、交叉口 LOS 评级表、车道级指标表、热力图截图嵌入；支持中文字体（SimHei.ttf）。 |
| 已完成 | ReportService PDF 支持 | `ReportService.export()` 移除 PDF 格式异常拒绝，format=PDF 时委托 `PdfReportService` 生成并返回 `application/pdf`；新增 `downloadPdf()` 方法支持已有报表的 PDF 重新生成。 |
| 已完成 | 报表详情接口 | 新增 `GET /api/reports/{id}` 返回 `ReportDetailDTO`（含 laneMetrics、intersectionLos、heatmapConfig、flightLineData 完整指标数据）。 |
| 已完成 | PDF 下载接口 | 新增 `GET /api/reports/{id}/download-pdf` 支持已有报表的 PDF 格式下载。 |
| 已完成 | OpenPDF 依赖 | pom.xml 新增 `com.github.librepdf:openpdf:2.2.2`（LGPL/MPL，Java 21+ 兼容）。 |
| 已完成 | 中文字体 | 内置 SimHei.ttf 到 `resources/fonts/`，OpenPDF 加载后支持中文标题/表头；字体文件已加入 .gitignore。 |
| 已完成 | PDF 生成单元测试 | `PdfReportServiceTest` 6 个测试：最小数据、含 LOS 数据、含车道指标、数组格式指标、完整数据、空 JSON 对象，均验证输出以 `%PDF-` 开头。 |
| 已完成 | Controller 测试扩展 | `ReportControllerTest` 新增 3 个测试：PDF 导出返回 application/pdf、报表详情接口成功、报表详情 404。 |
| 已验证 | Maven 全量测试 | 执行 `mvn test`，79 个测试全部通过（原 70 + 新增 9）。 |
| 已验证 | 编译检查 | 执行 `mvn compile` 通过。 |

## v0.1.6 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 字体文件未纳入 Git | SimHei.ttf 约 9.7MB，已加入 .gitignore。 | 部署时需单独放置到 resources/fonts/，或在 Dockerfile 中从系统字体复制。 |
| 报表指标仍为占位值 | `ReportSummary.networkLOS` 和 `averageDelay` 仍返回固定占位值。 | 后续按仿真/评估引擎输出计算真实摘要。 |
| 热力图截图依赖前端传 base64 | PDF 中热力图需要前端在 snapshot 中传入 heatmapImage base64 数据。 | 前端需在导出时捕获 Canvas 截图并编码传入。 |

## v0.1.5 变更记录

变更时间：2026-06-02 13:10:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 登录/注册 IP 限流 | 新增 `RateLimitFilter` + `RateLimiter` 内存令牌桶，按 IP 维度限制登录（10 次/分钟）和注册（5 次/分钟）请求频率，超限返回 429；限流参数通过 `app.rate-limit.*` 环境变量可配；支持 `X-Forwarded-For` / `X-Real-IP` 代理头解析。 |
| 已完成 | 限流桶自动清理 | `@Scheduled` 每 5 分钟清理过期限流桶条目，防止内存泄漏。 |
| 已完成 | Swagger 生产环境收敛 | `OpenApiConfig` 加 `@ConditionalOnProperty` 条件装配；新增 `application-prod.yml`，生产环境禁用 API 文档和 Swagger UI；`SecurityConfig` 按配置动态放行 Swagger 路径。 |
| 已完成 | JWT logout Token 黑名单 | JWT 生成时加入 `jti`（UUID）唯一标识；新增 `TokenBlacklist` 内存黑名单；`AuthService.logout()` 将当前 token jti 写入黑名单；`JwtAuthenticationFilter` 校验黑名单，已撤销 token 返回 401；黑名单按 token 过期时间自动清理。 |
| 已完成 | `CurrentUser` 扩展 | record 新增 `jti` 和 `expiresAt` 字段，保留三参数便捷构造函数兼容现有调用。 |
| 已完成 | `ErrorCode` 扩展 | 新增 `TOO_MANY_REQUESTS`（429）枚举值。 |
| 已完成 | 限流单元测试 | `RateLimiterTest` 5 个测试：限额内放行、超限拒绝、IP 隔离、过期清理、窗口重置。 |
| 已完成 | 黑名单单元测试 | `TokenBlacklistTest` 5 个测试：黑名单检测、未列入放行、null 处理、过期清理、幂等性。 |
| 已完成 | 安全加固集成测试 | `SecurityHardeningTest` 4 个 MockMvc 测试：登录限流 429、注册限流 429、logout 后 token 失效、Swagger 非生产可访问。 |
| 已完成 | JwtService 测试增强 | `JwtServiceTest` 新增 `jti` 和 `expiresAt` 断言。 |
| 已验证 | Maven 全量测试 | 执行 `mvn test`，70 个测试全部通过（原 56 + 新增 14）。 |
| 已验证 | 编译检查 | 执行 `mvn compile` 通过。 |

## v0.1.5 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 限流为内存方案 | 单实例有效，多实例部署时限流状态不共享。 | 后续如需多实例限流，引入 Redis + Bucket4j。 |
| Token 黑名单为内存方案 | 同上，多实例部署时黑名单状态不共享。 | 后续如需多实例黑名单，引入 Redis 存储。 |
| 前后端联调未执行 | 当前所有测试均为 MockMvc/H2 内存数据库。 | 配置环境变量后做 smoke test。 |

## v0.1.4 变更记录

变更时间：2026-06-02 09:00:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | Controller MockMvc 测试 | 新增 4 个 Controller 测试类（Auth/Project/Template/Report），共 33 个测试用例，覆盖成功路径、参数校验、资源不存在等场景。 |
| 已完成 | 测试基础设施 | 新增 MockAuth（SecurityContext RequestPostProcessor）、TestSecurityConfig（测试安全配置），统一 MockMvc 认证模式。 |
| 已完成 | Mapper H2 集成测试 | 新增 ProjectIntegrationTest 和 ReportIntegrationTest，共 11 个测试用例，覆盖 CRUD、JSON 字段读写、软删除过滤、用户隔离。 |
| 已完成 | H2 测试数据库适配 | 新增 schema-h2.sql（H2 兼容 DDL）和 application-test.yml（迁移到 test/resources）；JsonNodeTypeHandler 增加数据库类型检测，同时支持 PostgreSQL 和 H2。 |
| 已完成 | UUID 类型处理器 | 新增 UuidTypeHandler，将 java.util.UUID 映射为 VARCHAR(36)，兼容 PostgreSQL 和 H2。 |
| 已完成 | MybatisPlusConfig 可配置化 | 分页拦截器 DbType 改为从配置读取，H2 测试环境使用 DbType.H2。 |
| 已完成 | Docker 化 | 新增多阶段 Dockerfile、docker-compose.yml（app + PostgreSQL 17）和 .dockerignore。 |
| 已验证 | Maven 全量测试 | 执行 `mvn test`，56 个测试全部通过（原 12 + 新增 44）。 |
| 已验证 | 编译检查 | 执行 `mvn compile` 通过。 |

## v0.1.4 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 前后端联调未执行 | 当前所有测试均为 MockMvc/H2 内存数据库，未连接真实 Supabase。 | 配置环境变量后做 smoke test。 |
| Docker 构建未验证 | Dockerfile 和 docker-compose.yml 已创建但未在本地构建验证。 | 执行 `docker compose up --build` 验证。 |
| 登录/注册限流未实现 | 代码内暂无 IP/用户名维度限流。 | 部署网关或引入 Bucket4j/Redis。 |
| PDF 报表未实现 | 首版只提供 CSV。 | 后续接入 PDF 生成库。 |

## v0.1.1 设计文档补充夹角标注

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 信息 | FR1.9 夹角标注设计 | 系统详细设计说明书 V1.7 新增 FR1.9 测量标注与夹角标注功能定义，纯前端渲染功能（AngleAnnotation/AnglePair 值对象、向量点积算法、Sprite 渲染），后端无 API 变更。 |

## v0.1.0 变更记录

## v0.1.0 变更记录

变更时间：2026-05-27 15:39:42

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 后端工程初始化 | 创建 Java 21 + Spring Boot 3.3 Maven 工程，补充基础配置、README 与环境变量说明。 |
| 已完成 | 公共基础设施 | 新增统一响应 `{ code, message, data }`、全局异常处理、CORS、OpenAPI、MyBatis-Plus 和 JSONB 类型处理。 |
| 已完成 | JWT 鉴权 | 新增 Spring Security stateless 配置、JWT 生成/解析、认证过滤器和 BCrypt 密码编码。 |
| 已完成 | Auth MVP | 实现注册、登录、当前用户和登出接口，返回前端需要的 `token/userId/username/role`。 |
| 已完成 | 工程与快照 MVP | 实现工程列表、创建、详情、更新、软删除和 `PUT /api/projects/{id}/snapshot`，保存时同步更新当前工程并写入快照表。 |
| 已完成 | 模板只读接口 | 实现模板列表与详情接口，未知分类返回空列表，避免前端 `ROAD_SECTION` 与数据库枚举不一致时报错。 |
| 已完成 | 报表 MVP | 实现报表生成、列表、CSV 导出和下载接口，PDF 导出明确留待后续版本。 |
| 已验证 | Maven 测试 | 使用临时 Maven settings 绕过本机全局 settings.xml 解析问题，执行 `mvn test` 通过，3 个基础契约测试均通过。 |

## 当前注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 本机 Maven 全局配置异常 | `D:\DevelopmentTools\apache-maven-3.9.16\conf\settings.xml` 在 `</settings>` 后存在额外标签，直接 `mvn test` 会失败。 | 修复全局 settings.xml，或继续使用临时 settings 参数运行 Maven。 |
| 未连接真实 Supabase 验证 | 当前测试不依赖真实数据库，避免提交凭据。 | 配置环境变量后再做 Supabase 联调和接口 smoke test。 |
| PDF 报表未实现 | 首版只提供 CSV Blob。 | 后续接入 PDF 生成库和前端截图数据。 |
| 快照列表/回滚未实现 | 本轮优先打通保存闭环。 | 下一轮补 `GET /api/projects/{id}/snapshots` 和 restore。 |

## v0.1.1 变更记录

变更时间：2026-05-27 16:30:46

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 后端安全加固 | 移除生产默认 JWT/数据库密钥，要求 `JWT_SECRET` 环境变量长度不低于 32 字符。 |
| 已完成 | JWT 用户校验 | JWT 认证时重新查询未删除用户，避免仅信任 token 中的用户角色声明。 |
| 已完成 | 请求体限制 | 新增 `/api` 写请求体大小过滤器，覆盖 `Content-Length` 与分块传输，并对超限请求返回 413。 |
| 已完成 | JSON 解析约束 | 配置 Jackson `StreamReadConstraints`，限制嵌套深度、字符串长度和数字长度。 |
| 已完成 | 快照与报表负载校验 | 对工程快照和报表 JSON 数据增加 2MB 级服务层校验，并补充拒绝超大数据的单元测试。 |
| 已完成 | 列表查询收敛 | 工程列表和报表列表改为固定分页上限 100 条，避免无界 `selectList`。 |
| 已完成 | CSV 导出防护 | 对 CSV 单元格做公式注入中和，降低表格软件打开时报表内容触发公式的风险。 |
| 已完成 | 安全默认策略 | Spring Security 改为非显式路由默认拒绝；登录字段增加长度限制。 |
| 已验证 | Maven 测试 | 使用临时 Maven settings 执行 `mvn test`，5 个测试全部通过。 |
| 已验证 | 代码与安全审查 | 聚焦 Java 审查和安全审查均未发现 MVP 前必须修复的 CRITICAL/HIGH 问题。 |

## v0.1.1 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 登录/注册限流未实现 | 当前代码内未加入 IP/用户名维度限流。 | 部署网关或后续引入 Bucket4j/Redis 限流。 |
| Swagger 生产暴露策略未分环境 | 当前显式放行 OpenAPI/Swagger 便于联调。 | 上生产前按 profile 关闭或限制内网访问。 |
| Logout 为无状态成功响应 | JWT 未做服务端撤销，token 仍可用到过期。 | 后续增加 jti + Redis 黑名单，或缩短 access token 生命周期。 |
| 报表指标仍为 MVP 占位 | `ReportSummary` 当前仍返回基础占位 LOS/延误值。 | 下一轮按仿真/评估输出计算真实摘要。 |

## v0.1.2 变更记录

变更时间：2026-05-27 19:55:36

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 快照列表接口 | 新增 `GET /api/projects/{id}/snapshots`，按当前用户工程权限返回历史快照元数据列表。 |
| 已完成 | 快照回滚接口 | 新增 `POST /api/projects/{id}/snapshots/{snapshotId}/restore`，可将工程恢复到指定历史快照。 |
| 已完成 | 快照安全收敛 | 快照列表不再返回完整 `snapshotData`，避免一次请求暴露大量历史拓扑数据。 |
| 已完成 | 回滚数据校验 | 回滚前校验快照中 `topologyData` 与 `ruleData` 必须存在且为对象，避免损坏当前工程状态。 |
| 已完成 | 权限隔离 | 列表与回滚均先校验工程归属，快照查询同时绑定 `projectId`、`snapshotId` 和 `is_deleted = false`。 |
| 已验证 | Maven 测试 | 执行 `mvn -f D:/DevelopmentProducts/UrbanMicro-CAD/Backend/pom.xml test`，9 个测试全部通过。 |
| 已验证 | 代码与安全审查 | 聚焦复查未发现快照列表/回滚相关 CRITICAL/HIGH 问题。 |

## v0.1.2 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 快照详情接口未实现 | 当前列表只返回元数据，完整快照数据仅用于回滚。 | 如果前端需要预览历史快照，再新增单条详情接口并做权限与响应大小限制。 |
| 快照分页参数未开放 | 当前固定返回最近 100 条。 | 后续按前端需要增加 `page/pageSize` 并限制最大页大小。 |

## v0.1.3 变更记录

变更时间：2026-05-28 10:30:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | ROAD_SECTION 断面模板兼容 | `GET /api/templates?category=ROAD_SECTION` 返回后端内置断面模板，不再因数据库枚举缺少 `ROAD_SECTION` 而返回空列表。 |
| 已完成 | Cross-section fallback 接口 | 新增 `GET /api/templates/cross-sections` 和 `GET /api/templates/cross-sections/{id}`，按前端 fallback 契约直接返回 `CrossSectionProfile` JSON。 |
| 已完成 | 断面模板数据 | 内置默认双车道、城市主干路（含公交车道）和完整街道（慢行友好）三类断面，响应中同时提供 `profile` 与 `snapshotData.profile`。 |
| 已验证 | Maven 测试 | 执行 `mvn -f d:/DevelopmentProducts/UrbanMicro-CAD/Backend/pom.xml test`，12 个测试全部通过，覆盖 ROAD_SECTION 列表、断面详情、宽度一致性和未知分类不查库。 |

## v0.1.3 后续注意事项

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 用户自定义模板未实现 | 当前断面模板为后端运行时内置数据，不写入 `prj_template`。 | 明确 `CUSTOM` 模板归属策略和数据库字段后再实现 `POST /api/templates`。 |
| 数据库枚举仍无 ROAD_SECTION | 本轮刻意不修改 Supabase schema，避免影响现有模板枚举。 | 如后续需要持久化断面模板，再通过受控迁移扩展 enum 或拆分模板类型。 |
